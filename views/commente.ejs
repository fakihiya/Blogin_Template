<h1>All Posts</h1>
<% if (posts && posts.length > 0) { %>
  <ul>
    <% posts.forEach(post => { %>
      <li>
        <h2><a href="/posts/<%= post.id %>"><%= post.title %></a></h2>
        <p><%= post.content %></p>
      </li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No posts found.</p>
<% } %>